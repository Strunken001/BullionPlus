import 'package:flutter/cupertino.dart';
import 'package:get/get.dart';
import '../controller/data_bundles/data_bundles_screen_controller.dart';


class ShowOperatorAndBundle extends StatelessWidget {
  ShowOperatorAndBundle({super.key});

  final controller = Get.put(DataBundlesScreenController());

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Column(
          children: [

          ],
        )
      ],
    );
  }
}
